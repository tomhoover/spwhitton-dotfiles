#!/bin/sh

adt-run --changes "$1" --- schroot "$2"

# if adt-run's exit code is 8 then the package had no tests but
# this isn't a failure, so catch it
adtexit=$?
if [ $adtexit = 8 ]; then
    exit 0
else
    exit $adtexit
fi

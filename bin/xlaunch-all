#!/bin/bash
. $HOME/.shenv

if zenity --question --text "Launch Emacs, tmux and browser sessions?"; then
    xlaunch term
    sleep 1
    xlaunch term
    sleep 1
    xlaunch www
    xlaunch emacs
fi

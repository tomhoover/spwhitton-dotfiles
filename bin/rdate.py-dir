#!/usr/pkg/bin/bash

shopt -s nullglob

for arg in $*
do
    if [ ! -d "$arg" ]
    then
	echo "only specify dirs on the cmd line please"
	exit 1
    fi
    find $arg -type f -name "*txt" -exec $HOME/bin/rdate.py '{}' \;
done
